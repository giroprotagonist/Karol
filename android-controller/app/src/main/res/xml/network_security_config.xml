<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
	<!--
	  Karol DJ uses HTTP on the local LAN (tablet API on :3131).
	  Android cannot express RFC1918 CIDR ranges in domain-config, so cleartext
	  stays enabled for arbitrary host IPs entered by the user. Localhost is
	  listed explicitly for emulators and loopback.
	-->
	<base-config cleartextTrafficPermitted="true">
		<trust-anchors>
			<certificates src="system" />
		</trust-anchors>
	</base-config>
	<domain-config cleartextTrafficPermitted="true">
		<domain includeSubdomains="false">localhost</domain>
		<domain includeSubdomains="false">127.0.0.1</domain>
		<domain includeSubdomains="false">10.0.2.2</domain>
	</domain-config>
</network-security-config>
