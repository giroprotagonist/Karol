# Karol environment configuration
# Copy to .env and fill in your values
# .env is gitignored — never commit it

# Android signing
ANDROID_KEYSTORE_PASSWORD=karol123
ANDROID_KEY_ALIAS=karol
ANDROID_KEY_PASSWORD=karol123

# VLC HTTP interface
VLC_PASSWORD=karol

# Server
KAROL_API_PORT=3131
